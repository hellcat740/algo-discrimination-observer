# 算法歧视观测 · 公网上线说明（v0.4.0）

## 已完成

| 事项 | 状态 | 位置 |
|---|---|---|
| 双登录体系后端（用户免密 UUID + 管理员密钥哈希校验） | ✅ 本地端到端测试通过 | GitHub: `hellcat740/algo-observer-server` |
| 门户首页 `/`（用户入口 / 管理员入口 / 下载入口） | ✅ | 同上仓库 `api/app/static/landing.html` |
| 用户看板 `/me`（一键登录、个人统计、近 7 天趋势、记录分页） | ✅ | `me.html` |
| 管理员登录 `/admin-login` + 原控制台 `/console` | ✅ | `admin-login.html` + 原 `index.html` |
| 插件下载页 `/download` + 安装说明 + zip 下载 | ✅ | `download.html`，插件包在 `api/app/static/plugin.zip` |
| 插件 v0.4.0 公网版（内置服务器地址与上报密钥） | ✅ | 本目录 `release/算法歧视观测-edge插件-v0.4.0-公网版.zip` |
| 服务部署到 Vercel | ⚠️ 被权限拦截 | 见下方「一键上线步骤」 |

## 密钥（请妥善保存，勿提交到任何仓库）

- **管理员密钥**：`adm_0e606056c947a33fa94b9339`（仅你持有；服务端只存 SHA-256 哈希，代码库里是哈希值，泄不了）
- **上报密钥**：`ing_76b1aafdb8aa410c73c28139`（已内置在插件里，本来就是公开的，只放行数据写入）

## 一键上线步骤（约 5 分钟）

代码仓库已就绪：<https://github.com/hellcat740/algo-observer-server>（含 `api/` 全部后端、`vercel.json`、插件包）。

1. 打开 <https://vercel.com/new>，登录你的 bjtu-psyagent 团队账号。
2. 「Import Git Repository」→ 首次使用需点 **Install Vercel GitHub App**（一次性授权）→ 选择 `algo-observer-server` → **Import**。
3. Framework 自动识别为 Other/Python，不用改，直接 **Deploy**。
4. 部署完成后得到 `https://algo-discrimination-observer-xxxx.vercel.app`。

> ⚠️ **国内访问必看**：`*.vercel.app` 域名在国内普遍被 DNS 污染（本机实测解析到错误 IP）。
> 上线后请在 Vercel 项目 → Settings → Domains 绑定一个**自己的域名**（如 `observer.xxx.com`，
> 任意平台购买，约 ¥60/年；Vercel 自动签发 HTTPS）。绑定后国内即可正常访问，
> 同时把插件内置地址和新域名告诉我，我帮你重新打包插件。

## 验证清单（部署后跑一遍）

- [ ] `https://你的域名/health` 返回 `{"status":"ok","db":"up"}`
- [ ] 首页能打开，「一键进入我的看板」免密进入
- [ ] `/admin-login` 输入管理员密钥 → 进入 `/console` 能看到全部数据
- [ ] `/download` 能下载插件 zip
- [ ] 装插件后访问商品页采集，刷新看板能看到自己的记录

## 数据持久化（强烈建议，1 分钟）

当前 Vercel 用 SQLite 存 `/tmp`，**重新部署会清库**。生产用法：
Vercel 项目 → Storage/Integrations → 添加 **Neon Postgres**（免费档）→ 自动注入 `DATABASE_URL`
→ Redeploy。代码无需任何改动（SQLAlchemy 自动适配）。

## 本地开发

```bash
cd deploy  # 或克隆 algo-observer-server
pip install -r requirements.txt
uvicorn api.index:app --reload --port 8000   # 本地访问 http://127.0.0.1:8000
```

## 已知限制（先简单做的取舍）

- 用户身份 = 浏览器本地 UUID，换浏览器/清缓存即新身份（看板数据不跨设备）
- 管理员密钥长期有效，无登录态过期；泄露后需更换哈希并重新部署
- CORS 仍为 `*`（鉴权全走头部，无 Cookie 会话，风险可控）
